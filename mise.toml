[tools]
node = "22"

[env]
# gheart GitHub App (created via /api/setup on 2026-07-05). These override the
# credentials stored in data/gheart.json; the secret is age-encrypted in
# fnox.toml and needs the key in ~/.config/fnox/age.txt to decrypt.
GITHUB_APP_ID = "4219863"
GITHUB_APP_SLUG = "gheart-3"
GITHUB_APP_CLIENT_ID = "Iv23li7Ni8bAxRo9z7xu"
GITHUB_APP_CLIENT_SECRET = "{{ exec(command='fnox get GITHUB_APP_CLIENT_SECRET') }}"
